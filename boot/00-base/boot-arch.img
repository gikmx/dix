#! /usr/bin/env bash
! $DOTFILES    && echo "Dotfiles is required."  && exit 1
! sys.is_arch  && echo "Invalid platform."      && exit 1

echo "Updating system and installing dependencies…"
deps=(
	'moreutils'
	'dnsutils'
	'git'
	'wget'
	'unzip'
)
sudo pacman -Syu --noconfirm ${deps[@]}