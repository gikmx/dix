#! /usr/bin/env bash
! $DIX && >&2 echo "DIX404" && exit 1
! sys.is_linux && sys.error "$DIX_PKG:406" && exit 1


if [ ! -s "$NVM_DIR/nvm.sh" ]; then
	log.error "$DIX_PKG:NVM:404"
else
	source "$NVM_DIR/nvm.sh"
fi